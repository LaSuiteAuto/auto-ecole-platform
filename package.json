{
  "name": "auto-ecole-platform",
  "private": true,
  "scripts": {
    "lint:backend": "cd apps/backend && npm run lint",
    "test:backend": "cd apps/backend && npm run test",
    "lint:frontend": "cd apps/frontend && npm run lint",

    "check": "npm run lint:backend && npm run test:backend && npm run lint:frontend",
    "push": "npm run check"
  },
  "devDependencies": {
    "prettier": "^3.7.4"
  }
}
